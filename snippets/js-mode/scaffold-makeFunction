# -*- mode: snippet -*-
# name: scaffold makeFunction
# key: makefunc
# --
/**
 * Provide dependencies
 * @param {{
 *   $2: function
 * }} dependencies
 */
module.exports = function make${1:$(my/capitalize-first-char yas-text)}({ ${2:dependantFunction} }) {
  return ${1:theFunction}

  /**
   * @param {{
   * $3: string
   * }} param
   */
  async function $1 ({ ${3:parameter} }) {
    $0
  }
}
